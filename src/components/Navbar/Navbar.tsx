import { Component } from 'react'; 

type clearToken = {
    removeToken: () => void
}

class Navbar extends Component <clearToken, {}> {

    render () {
        return (
            <div>

            </div>
        )
    }
}

export default Navbar; 